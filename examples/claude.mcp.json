{
  "mcpServers": {
    "qwen-2": {
      "command": "npx",
      "args": [
        "-y",
        "@runapi.ai/qwen-2-mcp"
      ],
      "env": {
        "RUNAPI_API_KEY": "${RUNAPI_API_KEY}"
      }
    }
  }
}
